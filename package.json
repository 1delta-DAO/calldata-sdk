{
  "name": "calldata-sdk",
  "version": "0.0.1",
  "packageManager": "pnpm@10.10.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "test": "pnpm -r run test",
    "clean": "pnpm -r exec rimraf dist && pnpm store prune && rm -rf node_modules && rm -rf packages/**/node_modules"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "lodash": "^4.17.21",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@1delta/asset-registry": "^0.0.155",
    "@1delta/dex-registry": "^0.0.53",
    "@1delta/type-sdk": "^0.0.3",
    "@types/lodash": "^4.17.17",
    "axios": "^1.9.0"
  }
}